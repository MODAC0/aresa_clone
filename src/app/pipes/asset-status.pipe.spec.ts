import { AssetStatusPipe } from './asset-status.pipe';

describe('AssetStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
