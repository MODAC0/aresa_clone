import { MaskingTypePipe } from './masking-type.pipe';

describe('MaskingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MaskingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
